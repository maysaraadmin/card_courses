<div class="card">
    <a href="{{url}}" class="card-link">
        <img src="{{image_url}}" alt="{{fullname}}" class="card-image">
        <div class="card-content">
            <h3 class="card-title">{{fullname}}</h3>
            <div class="card-description">{{{summary}}}</div>
        </div>
    </a>
</div>